/**
 * @type {import('next').NextConfig}
 */
module.exports = {
    // Other configurations...
  
    // Enable static HTML export
    output: {
      // Set the path for the exported files (default is 'out')
      path: 'out',
    },
  };